"""declaration of important types"""

# types that we need:
# Hash32 (32 byte hash)
# PrivateKey (object of util.cryptoutil.PrivateKey, fixed length)
# PublicKey (obect of util.cryptoutil.PublicKey, fixed length)
# Uint (unsigned integer value of fixed lengt)
# Int (? do we need this?, fixed length)

from src.util.hexstring import Hexstring
HexString = Hexstring()


